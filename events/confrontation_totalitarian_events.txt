namespace = totalitarian_confrontations

# totalitarian
country_event = {
	id = totalitarian_confrontations.1
	title = totalitarian_confrontations.1
	desc = totalitarian_confrontations.1.desc
	hide_window = no

	is_triggered_only = no

	picture = GFX_evt_assembly_fight
	show_sound = event_super_explosion

	mean_time_to_happen = { months = 1 }
	trigger = {
		is_variable_set = current_totalitarian_support
		is_variable_set = current_ideological_purity
		
		check_variable = {
			which = current_totalitarian_support
			value > current_ideological_purity
		}
	}

	immediate = {
		export_resource_income_to_variable = { 
			resource = totalitarian_support 
			variable = current_totalitarian_support_income
		}

		# One years income
		multiply_variable = {
			which = current_totalitarian_support_income
			value = 12
		}

		add_resource = { 
			totalitarian_support = -1
			mult = current_totalitarian_support_income 
		}
	}

	option = {
		name = OK
	}
}